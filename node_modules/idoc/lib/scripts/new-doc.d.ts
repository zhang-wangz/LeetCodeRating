import type { ParsedArgs } from 'minimist';
interface Options extends Partial<ParsedArgs> {
    force?: boolean;
}
export declare function newDoc(filename: string, title: string, options?: Options): Promise<void>;
export {};
