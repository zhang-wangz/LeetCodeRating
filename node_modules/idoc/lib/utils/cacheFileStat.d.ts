import { type IFileDirStat } from 'recursive-readdir-files';
export type CacheFileOptions = {
    folder?: string;
    isSave?: boolean;
};
export declare class CacheFile {
    data: Record<string, Partial<IFileDirStat>>;
    path: string;
    filename: string;
    folder: string;
    isSave: boolean;
    constructor(folder?: string);
    init(isSave?: boolean): Promise<this>;
    load(): Promise<void>;
    save(): Promise<void>;
    getPath(rawPath: string): string;
    get(rawPath: string): Partial<IFileDirStat>;
    add(stat: IFileDirStat, update?: boolean): this;
    update(filepath: string): Promise<this>;
    remove(rawPath: string): void;
}
export declare const cacheFile: CacheFile;
