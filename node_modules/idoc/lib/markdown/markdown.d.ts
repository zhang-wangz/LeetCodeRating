import { type IFileDirStat } from 'recursive-readdir-files';
import { type MenuData, type Config, type SiteGlobalConfig } from '../utils/conf.js';
import { type Chapter } from '../utils/chapters.js';
export declare function formatHTML(html?: string): Promise<string>;
export interface PageConfig extends Omit<SiteGlobalConfig, 'menus'> {
    layout?: string;
    prevPage?: Chapter;
    nextPage?: Chapter;
    fileStat?: Partial<IFileDirStat> & {
        atimeStr?: string;
        mtimeStr?: string;
        ctimeStr?: string;
    };
}
export interface TemplateData extends Omit<Config, 'menus' | 'chapters'> {
    /** 相对路径 `../../` */
    RELATIVE_PATH?: string;
    /** 输出完整目录 `markdown/basic-syntax.html` */
    RESOLVE_PATH?: string;
    markdown?: string;
    html?: string;
    menus?: MenuData[];
    chapters?: Array<Chapter>;
    tocsTree?: Array<TocTree>;
}
export interface TocTree extends Toc {
    children?: Array<TocTree>;
}
export type Toc = {
    number?: number;
    label?: string;
    href?: string;
    class?: string;
};
export declare function createHTML(mdStr: string, fromPath: string, toPath: string): Promise<any>;
