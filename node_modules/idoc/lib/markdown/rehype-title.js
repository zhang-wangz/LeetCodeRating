import { visit } from 'unist-util-visit';
import { getTitle, getDescription } from './utils.js';
const rehypeTitle = (callback) => {
    return (tree) => {
        visit(tree, (node) => {
            if (node.type === 'root') {
                callback && callback(getTitle(node), getDescription(node));
            }
        });
    };
};
export default rehypeTitle;
